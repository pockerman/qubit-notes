# qubit-note: Part 8 | Communication | HTTP

## Overview

In this qubit note we will briefly discuss the HTTP protocol. This is the main protocol that most modern web applications
use for communication and most REST APIs rely upon.



## HTTP

The HTTP protocol is the foundation of data communication on the web. It defines how clients (like browsers) and servers communicate.
HTTP  is a request-response protocol. What this means is that the client has to initiate the communication by 
sending a request. The server processes this request and returns a response. Before, however, sending any requests to the
server, the client has to connect to it. This is usually done via <a href="2026-03-17-part-4-TCP_101.md">TCP </a>.
An HTTP request typically includes

- Method (what you want to do)
- URL (resource location)
- Headers (metadata, e.g. content type)
- Body (optional data)

The HTTP protocol defines an number of methods we can ask the HTTP server to perform:

- POST: This is used for creating new resources.
- GET: This is used for reading (getting) existing resources.
- PUT: This is used for updating existing resources. As a convention, a PUT request should contain the full and updated version of an existing resource.
- DELETE: This is used for deleting existing resources.
- PATCH: This is used for updating existing resources. A PATCH request only contains the modifications to an existing resource.


The response of the HTTP server includes:

- Status code
- Headers
- Body (actual content)

There also exist conventions regarding the returning HTTP status code of each client request. The
most popular HTTP status codes as well as their meanings are the following:

- 200 means that everything went well and the specified action was executed successfully.
- 201 means that the wanted resource was created.
- 202 means that the request was accepted and is currently being processed. This is usually
used when an action takes too much time to complete.
- 301 means that the requested resource has been moved permanently—the new URI should
be part of the response. This is rarely used in RESTful services because API versioning is
used instead.
- 400 means that there was a bad request and that you should change your initial request
before sending it again.
- 401 means that the client tried to access a protected request without authorization.
- 403 means that the client does not have the required permissions for accessing a resource
even though the client is properly authorized. In UNIX terminology, 403 means that the
current user does not have the required privileges to perform an action.
- 404 means that the resource was not found.
- 405 means that the client used a method that is not allowed by the type of resource.
- 500 means internal server error—it probably indicates a server failure.

---
**Remark:**

If you want to learn more about the HTTP protocol, you should visit RFC 7231 at
https://datatracker.ietf.org/doc/html/rfc7231.

---

The key characteristics of the HTTP protocol that one should be aware of are:

- Stateless: Each request is independent (no memory of previous requests)
- Text-based (mostly) Easy to read/debug (HTTP/1.1)
- Extensible via headers: Allows features like authentication, caching, etc.
- Can be secured using TLS, resulting in: HTTPS i.e. encrypted communication

Finally, there is a number of versions of the protocol:

- HTTP/1.1: standard, text-based
- HTTP/2: faster, multiplexing
- HTTP/3: uses QUIC (UDP-based), lower latency


## Summary

The HTTP is the core protocol used for communication on the web, enabling clients and servers to interact through a request–response model over connections typically established via TCP. A client sends a request containing a method (e.g., GET, POST, PUT), a URL, headers, and optionally a body, and the server responds with a status code, headers, and content. HTTP defines standard methods for creating, retrieving, updating, and deleting resources, along with widely used status codes that indicate the outcome of a request (such as 200 for success or 404 for not found). Key characteristics of HTTP include being stateless, largely text-based, extensible via headers, and securable through TLS (HTTPS). Over time, it has evolved from HTTP/1.1 to more efficient versions like HTTP/2 and HTTP/3, which improve performance and reduce latency.



## References